{
  "name": "skyfire-ascent",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check-assets": "ls -la public/ | grep -E \"\\.(png|jpg|jpeg)$\""
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
